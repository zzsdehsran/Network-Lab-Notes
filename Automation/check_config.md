# check_config.py

网络设备配置自动备份与差异审计脚本。

## 功能说明

该脚本从 `ip_list.txt` 读取设备 IP 地址或主机名，通过 SSH 登录设备并获取当前配置。

获取配置后，脚本会：

1. 将当前配置保存到 `configs/`
2. 查找该设备最近一份历史配置
3. 使用 `difflib` 比较新旧配置
4. 在 `reports/` 中生成设备审计报告
5. 生成当天所有设备的汇总报告

## 适用范围

脚本默认执行：
